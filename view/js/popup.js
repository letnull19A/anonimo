export var Client;
(function (Client) {
    var UI;
    (function (UI) {
        class PopupManager {
            constructor() {
            }
            ChangeState() {
            }
        }
        UI.PopupManager = PopupManager;
        class PopupState {
        }
        UI.PopupState = PopupState;
    })(UI = Client.UI || (Client.UI = {}));
})(Client || (Client = {}));
